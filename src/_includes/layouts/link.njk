{% extends "layouts/base.njk" %}

{% block content %}
  <article class="container flow">
    <h1>{{ title }}</h1>

    <a href="{{ url }}">Check it out</a>

    <section class="flow">
      {{ content | safe }}
    </section>
  </article>
{% endblock %}
